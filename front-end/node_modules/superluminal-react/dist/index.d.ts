import React from 'react';
import AssistantView from './components/AssistantView';
interface Props {
    authToken: string;
    style?: React.CSSProperties;
    userProfilePictureStyle?: React.CSSProperties;
    userMessageStyle?: React.CSSProperties;
    assistantMessageStyle?: React.CSSProperties;
    inputStyle?: React.CSSProperties;
    sendButtonStyle?: React.CSSProperties;
}
interface State {
}
declare class Superluminal extends React.Component<Props, State> {
    assistantViewRef: React.RefObject<AssistantView>;
    constructor(props: Props);
    render(): React.JSX.Element;
    setUser(user: {
        id: string;
        name: string;
    }): void;
}
export default Superluminal;
